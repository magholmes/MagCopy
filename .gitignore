# downloaded encoders - fetch with tools/fetch_binaries.py
bin/
# local state
settings.json
errors.log
crash.log
selftest.txt
# the single-instance lock, and the per-test ones the suite makes
*.lock
# test output (screenshots of a real desktop; never commit these)
tools/_shots/
tools/_*.png
tools/_*.py
# build
build/
dist/
*.spec
__pycache__/
*.pyc
.venv/

# Finder leaves these behind in any folder it has been shown
.DS_Store
